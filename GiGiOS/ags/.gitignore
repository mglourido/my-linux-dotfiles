# Ajustes/estado/secretos de usuario de AGS: movidos a ~/.config/gigios/, fuera
# del repo (bin/link.sh los migra; ver hypr/SETUP.md §14). config/ ya no forma
# parte del repo — se ignora para que dispositivos que aún lo tengan no lo re-añadan.
config/

# Generado / desechable — no versionar
# @girs/: stubs de tipos GObject, se regeneran con `ags types`
@girs/
# scratch/: scripts de inspección/pruebas desechables
scratch/
# out.css.map: source-map de depuración generado desde style.scss
out.css.map

#config de vscode (actualmente no se usa)
.vscode/